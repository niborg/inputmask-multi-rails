require 'spec_helper'

describe Inputmask::Multi::Rails do
  it 'has a version number' do
    expect(Inputmask::Multi::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
